export interface CustomerList {
  id: number;
  name: string;
  location: string;
  active: boolean;
}

export interface DisplayCustomerList {
  firstName: string;
  lastName: string;
  location: string;
  province: string;
}
